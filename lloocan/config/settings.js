// Global configuration for timings used across the experience.
const settings = {
  // How fast each character appears (milliseconds)
  typingSpeed: 60,
  // Pause after each line has been fully typed (milliseconds)
  pauseBetween: 3000,
  // Duration of fade out animation (milliseconds). Should match --fade-out-duration in CSS.
  fadeOutDuration: 800,
};